interface:
  display_name: "Sync Upstream"
  short_description: "Pull basecamp framework updates"
  default_prompt: "Use $sync-upstream to check for and pull framework updates from the upstream basecamp repo."
